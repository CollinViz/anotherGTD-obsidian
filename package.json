{
  "name": "another-gtd",
  "version": "0.1.0",
  "description": "An Obsidian GTD plugin where the notes are the source of truth: filing a task moves its line between the gtd bucket files, and data.json holds config only.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && eslint . && node esbuild.config.mjs production",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "test": "node test/run.mjs",
    "version": "node scripts/version-bump.mjs && git add manifest.json versions.json",
    "lint": "eslint ."
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "gtd",
    "tasks",
    "productivity"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "0.20.2",
    "eslint": "^9.39.5",
    "eslint-plugin-obsidianmd": "^0.4.1",
    "obsidian": "latest",
    "tslib": "2.6.2",
    "typescript": "5.4.5",
    "typescript-eslint": "^8.65.0"
  }
}
